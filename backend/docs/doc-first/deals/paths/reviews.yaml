offerReviews:
  get:
    tags:
      - Reviews
    summary: Получить отзывы по offer
    description: |
      Возвращает отзывы, оставленные по конкретному `offer` после завершенных сделок.

      Особенности:
      - отзывы возвращаются в порядке от новых к старым;
      - сюда попадают только отзывы, у которых заполнен `offerRef`;
      - если отзыв одновременно относится и к `item`, в ответе будет заполнен `itemRef`,
        чтобы было понятно, о какой модифицированной позиции шла речь.
    operationId: getOfferReviews
    security:
      - bearerAuth: [ ]
    parameters:
      - name: offerId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    responses:
      '200':
        description: Список отзывов по offer
        content:
          application/json:
            schema:
              $ref: '../components/responses.yaml#/GetOfferReviewsResponse'
      '400':
        description: Invalid offer ID
        content:
          application/json:
            schema:
              $ref: '../components/responses.yaml#/ErrorResponse'
      '401':
        description: Unauthorized
        content:
          application/json:
            schema:
              $ref: '../components/responses.yaml#/ErrorResponse'
      '404':
        description: Offer not found
        content:
          application/json:
            schema:
              $ref: '../components/responses.yaml#/ErrorResponse'
      '500':
        description: Internal server error
        content:
          application/json:
            schema:
              $ref: '../components/responses.yaml#/ErrorResponse'



dealItemReviews:
  get:
    tags:
      - Reviews
    summary: Получить отзывы по item
    description: |
      Возвращает отзывы, относящиеся к конкретному `item` завершенной сделки.

      Особенности:
      - отзывы возвращаются в порядке от новых к старым;
      - если отзыв относится только к `item`, будет заполнен только `itemRef`;
      - если отзыв относится и к `offer`, и к `item`, в ответе будут заполнены обе ссылки.
    operationId: getDealItemReviews
    parameters:
      - name: dealId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: itemId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    security:
      - bearerAuth: [ ]
    responses:
      '200':
        description: Список отзывов по item
        content:
          application/json:
            schema:
              $ref: '../components/responses.yaml#/GetItemReviewsResponse'
      '400':
        description: Invalid path parameters
        content:
          application/json:
            schema:
              $ref: '../components/responses.yaml#/ErrorResponse'
      '401':
        description: Unauthorized
        content:
          application/json:
            schema:
              $ref: '../components/responses.yaml#/ErrorResponse'
      '404':
        description: Сделка или item не найдены
        content:
          application/json:
            schema:
              $ref: '../components/responses.yaml#/ErrorResponse'
      '500':
        description: Internal server error
        content:
          application/json:
            schema:
              $ref: '../components/responses.yaml#/ErrorResponse'
  post:
    tags:
      - Reviews
    summary: Оставить отзыв о позиции сделки
    description: |
      Оставить отзыв по `item` завершенной сделки. Конкретная цель отзыва определяется автоматически
      по полям `items.offer_id` и `items.updated_at`.

      Бизнес-правила:
      - отзыв можно оставить только если сделка находится в статусе `Completed`;
      - отзыв может оставить только участник этой сделки, являющийся получателем item;
      - в отзыве сохраняется информация об отправителе товара/услуги
      - один пользователь может оставить не более одного отзыва на один и тот же набор целей в рамках сделки.

      При создании отзыва:
      - если `items.offer_id IS NULL`, отзыв создается только на `item`;
      - если `items.updated_at IS NULL`, отзыв создается только на `offer`;
      - если `items.offer_id IS NOT NULL` и `items.updated_at IS NOT NULL`, отзыв создается одновременно на `offer` и `item`.
    operationId: createDealItemReview
    parameters:
      - name: dealId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: itemId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    security:
      - bearerAuth: [ ]
    requestBody:
      required: true
      content:
        application/json:
          schema:
            $ref: '../components/requests.yaml#/CreateReviewRequest'
    responses:
      '201':
        description: Отзыв создан
        content:
          application/json:
            schema:
              $ref: '../components/schemas.yaml#/Review'
      '400':
        description: |
          Некорректный запрос:
          - сделка не находится в статусе `Completed`;
          - рейтинг вне диапазона `1..5`.
        content:
          application/json:
            schema:
              $ref: '../components/responses.yaml#/ErrorResponse'
      '401':
        description: Unauthorized
        content:
          application/json:
            schema:
              $ref: '../components/responses.yaml#/ErrorResponse'
      '403':
        description: |
          Операция запрещена:
          - пользователь не является участником сделки;
          - пользователь пытается оставить отзыв на собственный offer в кейсах, где отзыв относится к offer.
        content:
          application/json:
            schema:
              $ref: '../components/responses.yaml#/ErrorResponse'
      '404':
        description: Сделка или item не найдены
        content:
          application/json:
            schema:
              $ref: '../components/responses.yaml#/ErrorResponse'
      '409':
        description: Пользователь уже оставил отзыв для этого набора целей в рамках данной сделки
        content:
          application/json:
            schema:
              $ref: '../components/responses.yaml#/ErrorResponse'