VITE_YANDEX_MAPS_API_KEY=realkey

# Таймаут ожидания ответа от LLM-ранжирования (мс).
# Если за это время /suitable-for/{id}/ranged не ответил — используется обычный /suitable-for/{id}.
VITE_LLM_RANGED_TIMEOUT_MS=5000
